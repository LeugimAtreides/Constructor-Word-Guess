// Set valuable to require inquirer
var inquire = require("inquirer");

// index.js: The file containing the logic for the course of the game,

// which depends on Word.js
var word = require("./word.js");

// and:

// Randomly selects a word and uses the Word constructor to store it
function selectWord(){};

// Prompts the user for each guess 
function playGame(){};

// and keeps track of the user's remaining guesses
function keepTrack(){};